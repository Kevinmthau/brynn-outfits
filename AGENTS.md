# AGENTS.md

## Workflow

- Work directly on `main` by default (no feature branches) unless the user explicitly asks for a branch.
- Prefer small, focused commits with clear messages.
- Run quick sanity checks when applicable (e.g. `node -c` for JS changes, `python -m py_compile` for Python).

